# 09 — What is still unknown

Generated by `tools/mk1doc_unknowns.py` from the other fact files, so
it cannot drift away from the measurements behind it.
Machine-readable: `facts/unknowns.json`.

**12 open items**: emulation gap 11, symbols 1

Each says what IS known, what is not, and how to resolve it — an entry
here is a piece of work, not a shrug.

## emulation gap

### DSPI0 (0xFC05C000) is referenced but not modelled

- **known:** 313 references across 5 distinct offsets
- **unknown:** what the firmware expects back from it, and whether its absence changes behaviour
- **how to resolve:** hook reads of the block and see what the firmware does with the value

### GPIO ports (0xEC094000) is referenced but not modelled

- **known:** 192 references across 44 distinct offsets
- **unknown:** what the firmware expects back from it, and whether its absence changes behaviour
- **how to resolve:** hook reads of the block and see what the firmware does with the value

### USB OTG (ChipIdea/EHCI) (0xFC0B0000) is referenced but not modelled

- **known:** 143 references across 22 distinct offsets
- **unknown:** what the firmware expects back from it, and whether its absence changes behaviour
- **how to resolve:** hook reads of the block and see what the firmware does with the value

### DSPI1 (0xFC03C000) is referenced but not modelled

- **known:** 47 references across 7 distinct offsets
- **unknown:** what the firmware expects back from it, and whether its absence changes behaviour
- **how to resolve:** hook reads of the block and see what the firmware does with the value

### INTC1 (0xFC04C000) is referenced but not modelled

- **known:** 31 references across 13 distinct offsets
- **unknown:** what the firmware expects back from it, and whether its absence changes behaviour
- **how to resolve:** hook reads of the block and see what the firmware does with the value

### INTC2 (0xFC050000) is referenced but not modelled

- **known:** 26 references across 11 distinct offsets
- **unknown:** what the firmware expects back from it, and whether its absence changes behaviour
- **how to resolve:** hook reads of the block and see what the firmware does with the value

### UART9 (INTC1 source 53, vector 181) (0xEC074000) is referenced but not modelled

- **known:** 20 references across 6 distinct offsets
- **unknown:** what the firmware expects back from it, and whether its absence changes behaviour
- **how to resolve:** hook reads of the block and see what the firmware does with the value

### SCM (system control; holds PPMSR/PPMCR/WCR) (0xFC040000) is referenced but not modelled

- **known:** 19 references across 3 distinct offsets
- **unknown:** what the firmware expects back from it, and whether its absence changes behaviour
- **how to resolve:** hook reads of the block and see what the firmware does with the value

### CCM / chip configuration (0xEC090000) is referenced but not modelled

- **known:** 15 references across 4 distinct offsets
- **unknown:** what the firmware expects back from it, and whether its absence changes behaviour
- **how to resolve:** hook reads of the block and see what the firmware does with the value

### EPORT0 (edge port) (0xFC090000) is referenced but not modelled

- **known:** 12 references across 3 distinct offsets
- **unknown:** what the firmware expects back from it, and whether its absence changes behaviour
- **how to resolve:** hook reads of the block and see what the firmware does with the value

### INTC0 (0xFC048000) is referenced but not modelled

- **known:** 12 references across 8 distinct offsets
- **unknown:** what the firmware expects back from it, and whether its absence changes behaviour
- **how to resolve:** hook reads of the block and see what the firmware does with the value

## symbols

### symbol px_copy unresolved

- **known:** rule Sig; 0 match(es) for masked signature (32 bytes), need exactly 1
- **unknown:** its address in this image
- **how to resolve:** find the mk1 equivalent of the signature, which matches 0 times here

