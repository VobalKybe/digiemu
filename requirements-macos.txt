# Runtime/build dependencies for the native macOS app.
# sounddevice uses PortAudio for CoreAudio output and RawOutputStream keeps the
# emulator free of a NumPy dependency.
sounddevice==0.5.2
pyinstaller==6.19.0
pyinstaller-hooks-contrib==2026.4
altgraph==0.17.5
packaging==26.0
